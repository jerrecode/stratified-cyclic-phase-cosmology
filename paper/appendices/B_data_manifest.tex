\section{Data-manifest contract}
The authoritative registry is \texttt{data/releases.yaml}, validated against \texttt{data/manifest.schema.json}. For each analysis, a run lock must record selected products, resolved URLs, provider versions, citations, acknowledgements, local paths, and checksums. Query-based archives additionally require the exact ADQL, SQL, GPS-segment, sky-region, or basket specification.