\section{Discussion and falsification criteria}
The canonical model is rejected as a cyclic theory if no stable nonsingular cycles occur in the declared parameter domain. Any extension must preserve stress-energy conservation, possess a controlled standard-model limit, and satisfy ghost and gradient-stability conditions. Spectral features are treated as physical only if they converge under resolution, sampling, and solver changes and if a derived transfer function connects them to an observable.